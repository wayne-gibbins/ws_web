services:
    - redis
    - id: mattapi
      url: file://../api#dev
      cmd: python /pipeline/source/main.py
box: python:2.7
build:
  steps:
    - pip-install
    - script:
        name: start web server
        code: |
            python app.py
