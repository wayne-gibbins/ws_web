services:
    - redis
    - id: mattapi
      url: file://../api#dev
      cmd: /bin/bash -c "python $WERCKER_SOURCE_DIR/main.py"
box: python:2.7
build:
  steps:
    - pip-install
    - script:
        name: start web server
        code: |
            python app.py
