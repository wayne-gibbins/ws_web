services:
    - redis
    - id: mattapi
      url: file://../api#dev
      cmd: python /pipeline/source/main.py
box: python:2.7
dev:
  steps:
    - pip-install
    - internal/watch:
        name: start web server
        code: python app.py
        reload: false
